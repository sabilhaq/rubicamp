<%- include("../partials/header") -%>
</head>

<body>
  <%- include("../partials/navbar") -%>

  <div class="container pt-3" style="max-width: none;">
    <h1>Add Project</h1>
    <hr>

    <div class="container" style="max-width: none;">
      <form id="form-edit" action="" method="POST" class="m-auto w-75" style="max-width: 1000px;">
        <div class="row mb-3 align-items-center justify-content-between">
          <div class="col-auto">
            <label class="form-check-label" for="name">Name</label>
          </div>
          <div class="col-auto w-75">
            <input type="text" id="name" name="name" class="form-control" placeholder="Project Name">
          </div>
        </div>

        <div class="row mb-3 align-items-center justify-content-between">
          <div class="col-auto align-self-start">
            <label class="form-check-label" for="member">Members</label>
          </div>
          <div class="col-auto w-75">
            <% for( let i = 0; i < memberOptions.length; i++ ) { %>

            <div class="row mb-3 align-items-center justify-content-between">
              <div class="col-auto">
                <input type="checkbox" class="form-check-input" id="<%= memberOptions[i].userid %>" name="<%= memberOptions[i].userid %>">
                <label class="form-check-label" for="<%= memberOptions[i].userid %>"><%= memberOptions[i].firstname %></label>
              </div>
            </div>

            <% } %>
          </div>
        </div>

        <button class="btn btn-primary" type="submit">Save</button>
      </form>
    </div>
  </div>
</body>

<%- include("../partials/footer") -%>