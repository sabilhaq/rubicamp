<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/projects">Project Management System</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= nav == "projects" && "active" %>" aria-current="page" href="/projects">Projects</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= nav == "profile" && "active" %>" href="/profile">Profile</a>
        </li>
        <% if (userrole == "admin") { %>

        <li class="nav-item">
          <a class="nav-link <%= nav == "users" && "active" %>" href="/users">Users</a>
        </li>

        <% } %>
      </ul>
      <form action="/logout" method="POST" class="d-flex">
        <button class="btn btn-outline-success" type="submit">Sign out</button>
      </form>
    </div>
  </div>
</nav>